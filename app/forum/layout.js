
export const metadata = {
  title: "Main - JustChat 💬",
  description: "A place to chat with friends and strangers anonymously. And Discuss anything you want! Join the conversation now.",
};

export default function ForumLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}
